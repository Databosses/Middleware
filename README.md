# Databosses Middleware

This is a Python Flask application which would interface the frontend to the backend SQLite DB.

## Installing Prerequisites
```
pip install -r ./requirements.txt
```

## Starting The Application
```
python ./app.py
```

